name: infra-cd

on:
  workflow_call:
    outputs:
      EC2_PUBLIC_IP:
        value: ${{ jobs.apply.outputs.EC2_PUBLIC_IP }}
      CLOUDFRONT_ID:
        value: ${{ jobs.apply.outputs.CLOUDFRONT_ID }}
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "infra/**"
      - ".github/workflows/infra-ci.yml"
      - ".github/workflows/infra-cd.yml"
      - ".github/workflows/scripts/**"
      - ".github/actions/setup-aws-terraform/**"



concurrency:
  group: production-infrastructure
  cancel-in-progress: false

jobs:
  run-ci:
    if: vars.CICD_ENABLED != 'false'
    permissions:
      id-token: write
      contents: read
      pull-requests: write
    uses: ./.github/workflows/infra-ci.yml
    with:
      environment: prod
    secrets: inherit

  apply:
    environment: prod
    permissions:
      id-token: write
      contents: read
    outputs:
      EC2_PUBLIC_IP: ${{ steps.tf_out.outputs.EC2_PUBLIC_IP }}
      CLOUDFRONT_ID: ${{ steps.tf_out.outputs.CLOUDFRONT_ID }}

    needs: run-ci
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./infra

    steps:
      - uses: actions/checkout@v7
        name: Checkout Files

      - name: Setup AWS and Terraform
        uses: ./.github/actions/setup-aws-terraform
        with:
          aws-region: ap-south-1
          role-to-assume: ${{ vars.INFRA_ROLE_ARN }}
          working-directory: "./infra"

      - name: Terraform Apply
        run: terraform apply -no-color -input=false -auto-approve

      - name: Output Resource Details
        id: tf_out
        run: |
          echo "EC2_PUBLIC_IP=$(terraform output -raw ec2_public_ip)" >> $GITHUB_OUTPUT
          echo "CLOUDFRONT_ID=$(terraform output -raw cloudfront_distribution_id)" >> $GITHUB_OUTPUT

      - name: Update Repo Variables
        env:
          GH_TOKEN: ${{ secrets.GH_ACCESS_TOKEN }}
        run: |
          gh variable set EC2_PUBLIC_IP --body "${{ steps.tf_out.outputs.EC2_PUBLIC_IP }}"
          gh variable set CLOUDFRONT_ID --body "${{ steps.tf_out.outputs.CLOUDFRONT_ID }}"

      - name: SCP scripts
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ steps.tf_out.outputs.EC2_PUBLIC_IP }}
          username: admin
          key: ${{ secrets.SSH_KEY }}
          source: ".github/workflows/scripts/configure-server.sh"
          target: "/tmp/mint-backend/"
          strip_components: 3

      - name: Configure EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ steps.tf_out.outputs.EC2_PUBLIC_IP }}
          username: admin
          key: ${{ secrets.SSH_KEY }}
          script: |
            sudo mkdir -p /opt/mint-backend/config/
            sudo cp -r /tmp/mint-backend/ /opt/
            sudo bash /opt/mint-backend/configure-server.sh
